spring:
  application:
    name: library-management

  datasource:
    url: jdbc:sqlserver://localhost:1433;databaseName=LibraryManagement;encrypt=true;trustServerCertificate=true;sendStringParametersAsUnicode=false
    username: library_user
    password: 123456
    driver-class-name: com.microsoft.sqlserver.jdbc.SQLServerDriver

  jpa:
    hibernate:
      ddl-auto: update
    defer-datasource-initialization: true

    show-sql: true

    properties:
      hibernate:
        format_sql: true

    database-platform: org.hibernate.dialect.SQLServerDialect

  sql:
    init:
      mode: always
      encoding: UTF-8

  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest

  data:
    redis:
      host: localhost
      port: 6379

  cache:
    type: redis
    redis:
      time-to-live: 3600000  

  mail:
    host: smtp.gmail.com
    port: 587
    username: tuannguyenanh2357@gmail.com
    password: uaglwnmfcxzfogep
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

server:
  port: 8080

jwt:
  signerKey: "1234567890123456789012345678901234567890123456789012345678901234"

app:
  library:
    fine:
      default-daily-amount: 5000
      default-replacement-fee: 200000
    borrowing:
      max-renewals: 1
      renewal-extension-days: 7
      default-days: 14
      max-days: 30
  setup:
    admin:
      username: ${ADMIN_USERNAME:admin}
      password: ${ADMIN_PASSWORD:admin123}
    librarian:
      username: ${LIBRARIAN_USERNAME:librarian}
      password: ${LIBRARIAN_PASSWORD:librarian123}

